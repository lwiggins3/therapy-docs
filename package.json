{
  "name": "therapy-docs",
  "private": true,
  "version": "0.1.0",
  "description": "Therapist post-session aid recommender — GCP-hosted, HIPAA-scoped mono-repo",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "eslint": "^10.8.0",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
